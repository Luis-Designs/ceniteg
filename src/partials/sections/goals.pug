include ../../data/goals

each goal, key in goals
    .card.mb-5.goal-card
        img.card-img-top(src=goal.img alt=goal.alt)
        .card-body
            h5.card-title #{goal.title}
            p.card-text #{goal.description}